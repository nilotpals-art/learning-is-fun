import{createExamResultPdf}from"@/features/learning-planner/services/exam-result-pdf-service";import{requireRole}from"@/lib/auth/services/auth-service";import{DASHBOARD_ROLES}from"@/lib/navigation";
export async function GET(request:Request,{params}:{params:Promise<{eventId:string}>}){const profile=await requireRole(DASHBOARD_ROLES),{eventId}=await params,studentId=new URL(request.url).searchParams.get("studentId")??undefined,pdf=await createExamResultPdf(profile,eventId,studentId);if(!pdf)return new Response("Published result not found.",{status:404});return new Response(Buffer.from(pdf),{headers:{"Content-Type":"application/pdf","Content-Disposition":`attachment; filename="exam-result-${eventId}.pdf"`,"Cache-Control":"private, no-store"}})}
