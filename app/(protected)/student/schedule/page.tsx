import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { StudentPageShell } from "@/features/student-dashboard/components/student-page-shell";
import { getStudentSchedule, getStudentScheduleHolidays } from "@/features/student-dashboard/services/student-dashboard-service";
import { requireRole } from "@/lib/auth/services/auth-service";

export default async function StudentSchedulePage() { const profile=await requireRole(["Student"]); const [events,holidays]=await Promise.all([getStudentSchedule(profile),getStudentScheduleHolidays(profile).catch(()=>[])]); return <StudentPageShell title="My Schedule" description="Your classes and learning events for the next 30 days.">{holidays.length?<div className="space-y-2">{holidays.map(holiday=><div key={holiday.id} className="rounded-xl border border-amber-300 bg-amber-50 p-3 text-sm text-amber-950 dark:bg-amber-950/20 dark:text-amber-100"><strong>Holiday: {holiday.name}</strong> · {holiday.date}</div>)}</div>:null}{events.length ? <div className="grid gap-3 lg:grid-cols-2">{events.map((event) => <Card key={event.id}><CardContent className="p-5"><div className="flex items-start justify-between gap-3"><div><p className="font-bold">{event.title}</p><p className="mt-1 text-sm text-muted-foreground">{event.eventDate} · {event.startTime ?? "All day"}{event.endTime ? `–${event.endTime}` : ""}</p></div><Badge variant="outline">{event.status}</Badge></div><p className="mt-3 text-sm text-muted-foreground">{[event.subjectName,event.batchName,event.scheduleType.replaceAll("_"," ")].filter(Boolean).join(" · ")}</p></CardContent></Card>)}</div> : <Card><CardContent className="p-8 text-center text-muted-foreground">No upcoming schedule events.</CardContent></Card>}</StudentPageShell>; }
