import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import type { PublicHoliday } from "@/features/learning-planner/types/learning-planner";

export function HolidayCalendarList({holidays,providerAvailable=true}:{holidays:PublicHoliday[];providerAvailable?:boolean}){return <section className="space-y-3" aria-labelledby="holiday-calendar-heading"><div><h2 id="holiday-calendar-heading" className="text-xl font-semibold">Holidays</h2><p className="text-sm text-muted-foreground">Public holidays are display-only until an Administrator imports them.</p></div>{!providerAvailable&&<p role="status" className="rounded-xl border border-amber-300 bg-amber-50 p-3 text-sm text-amber-900">The public holiday provider is temporarily unavailable. Imported and institute holidays remain available.</p>}<div className="grid gap-3 md:grid-cols-2">{holidays.length===0?<Card><CardContent className="p-6 text-sm text-muted-foreground">No holidays in this date range.</CardContent></Card>:holidays.map(holiday=><Card key={holiday.id} className="border-l-4 border-l-amber-500"><CardHeader className="pb-2"><div className="flex flex-wrap items-start justify-between gap-2"><CardTitle className="text-base">{holiday.name}</CardTitle><Badge variant="outline">{holiday.scope==="national"?"India Holiday":holiday.scope==="state"?"State Holiday":holiday.scope==="branch"?"Branch Holiday":"Institute Holiday"}</Badge></div></CardHeader><CardContent className="flex flex-wrap items-center gap-2 text-sm"><span>{holiday.date}</span>{holiday.source==="external"?<Badge variant="secondary">Display only</Badge>:holiday.observedAsHoliday?<Badge>Non-working</Badge>:<Badge variant="outline">Classes held</Badge>}</CardContent></Card>)}</div></section>}
