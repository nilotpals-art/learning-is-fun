import Link from"next/link";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";
export function ExamResultsList({events}:{events:Array<{id:string;title:string;eventDate:string;status:string;batchName:string;subjectName:string|null;resultStatus:string|null}>}){return <div className="grid gap-4 lg:grid-cols-2">{events.length?events.map(e=><Card key={e.id}><CardHeader><div className="flex items-start justify-between gap-3"><CardTitle>{e.title}</CardTitle><Badge variant="outline">{e.resultStatus??"Not started"}</Badge></div></CardHeader><CardContent className="space-y-3 text-sm"><p>{e.eventDate} · {e.batchName} · {e.subjectName??"Subject not set"}</p><Button nativeButton={false} render={<Link href={`/learning-planner/exam-results/${e.id}`}/>}>{e.resultStatus==="published"?"View Results":"Enter Marks"}</Button></CardContent></Card>):<Card><CardContent className="p-8 text-center text-muted-foreground">No active Planner Exam events are available.</CardContent></Card>}</div>}
