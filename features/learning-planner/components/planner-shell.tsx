import Link from "next/link";
import { Button } from "@/components/ui/button";
export function PlannerShell({ title, description, children }: { title: string; description: string; children: React.ReactNode }) { return <div className="space-y-6"><div className="flex flex-col gap-4 rounded-3xl bg-gradient-to-r from-indigo-600 to-violet-600 p-6 text-white shadow-lg sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-indigo-100">Learning Planner</p><h1 className="mt-2 text-3xl font-bold">{title}</h1><p className="mt-2 max-w-2xl text-indigo-100">{description}</p></div><nav className="flex flex-wrap gap-2" aria-label="Learning Planner"><Button nativeButton={false} variant="secondary" render={<Link href="/learning-planner" />}>Overview</Button><Button nativeButton={false} variant="secondary" render={<Link href="/learning-planner/calendar" />}>Calendar</Button><Button nativeButton={false} variant="secondary" render={<Link href="/learning-planner/schedules" />}>Schedules</Button><Button nativeButton={false} variant="secondary" render={<Link href="/learning-planner/events" />}>Events</Button><Button nativeButton={false} variant="secondary" render={<Link href="/learning-planner/holidays" />}>Holidays</Button><Button nativeButton={false} variant="secondary" render={<Link href="/learning-planner/exam-results" />}>Exam Results</Button></nav></div>{children}</div>; }
